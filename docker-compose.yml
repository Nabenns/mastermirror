version: '3.8'
services:
  app:
    build: .
    container_name: discord-autoforwarder
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: always
    environment:
      - NODE_ENV=production
