<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Channels - Discord Auto-Forwarder</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>

<body>
    <nav class="navbar navbar-expand-lg sticky-top">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="bi bi-robot"></i> Discord Auto-Forwarder
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">
                            <i class="bi bi-speedometer2 me-1"></i> Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/servers">
                            <i class="bi bi-hdd-network me-1"></i> Servers
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/channels">
                            <i class="bi bi-hash me-1"></i> Channels
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logs">
                            <i class="bi bi-journal-text me-1"></i> Logs
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4 fade-in">
        <div class="row g-4">
            <div class="col-lg-8">
                <div class="card h-100">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h5 class="mb-0"><i class="bi bi-hash me-2"></i>Configured Channels</h5>
                        <span class="badge bg-primary rounded-pill">
                            <%= channels ? channels.length : 0 %> Total
                        </span>
                    </div>
                    <div class="card-body">
                        <% if (!channels || channels.length===0) { %>
                            <div class="text-center py-5 text-muted">
                                <i class="bi bi-chat-square-text display-1 mb-3 d-block opacity-25"></i>
                                <h5>No Channels Configured</h5>
                                <p>Add a channel to start forwarding messages.</p>
                            </div>
                            <% } else { %>
                                <div class="row g-3">
                                    <% channels.forEach(channel=> { %>
                                        <div class="col-12">
                                            <div class="webhook-item">
                                                <div class="d-flex justify-content-between align-items-start mb-2">
                                                    <div>
                                                        <h5 class="mb-1 text-light">#<%= channel.name %>
                                                        </h5>
                                                        <small class="text-muted"><i class="bi bi-hdd-network me-1"></i>
                                                            <%= channel.server_name %>
                                                        </small>
                                                    </div>
                                                    <span
                                                        class="badge <%= channel.is_forwarding ? 'bg-success' : 'bg-danger' %> bg-opacity-25 text-<%= channel.is_forwarding ? 'success' : 'danger' %> border border-<%= channel.is_forwarding ? 'success' : 'danger' %> border-opacity-25">
                                                        <%= channel.is_forwarding ? 'Forwarding' : 'Paused' %>
                                                    </span>
                                                </div>

                                                <div class="mb-3">
                                                    <div
                                                        class="d-flex align-items-center gap-2 p-2 bg-black bg-opacity-25 rounded border border-secondary border-opacity-25">
                                                        <i class="bi bi-link-45deg text-muted"></i>
                                                        <span class="text-truncate text-muted small"
                                                            style="max-width: 300px;">
                                                            <%= channel.webhook_url %>
                                                        </span>
                                                        <button class="btn btn-link btn-sm p-0 ms-auto text-secondary"
                                                            onclick="copyToClipboard('<%= channel.webhook_url %>')"
                                                            title="Copy URL">
                                                            <i class="bi bi-clipboard"></i>
                                                        </button>
                                                    </div>
                                                </div>

                                                <div class="d-flex gap-2">
                                                    <form action="/channels/<%= channel.id %>/toggle" method="POST"
                                                        class="flex-grow-1">
                                                        <button type="submit"
                                                            class="btn btn-sm w-100 <%= channel.is_forwarding ? 'btn-outline-danger' : 'btn-outline-success' %>">
                                                            <i
                                                                class="bi <%= channel.is_forwarding ? 'bi-pause-circle' : 'bi-play-circle' %> me-1"></i>
                                                            <%= channel.is_forwarding ? 'Pause' : 'Resume' %>
                                                        </button>
                                                    </form>
                                                    <form action="/channels/<%= channel.id %>/delete" method="POST"
                                                        onsubmit="return confirm('Delete this channel configuration?')">
                                                        <button type="submit" class="btn btn-sm btn-outline-danger"
                                                            title="Delete">
                                                            <i class="bi bi-trash"></i>
                                                        </button>
                                                    </form>
                                                </div>
                                            </div>
                                        </div>
                                        <% }); %>
                                </div>
                                <% } %>
                    </div>
                </div>
            </div>

            <div class="col-lg-4">
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0"><i class="bi bi-plus-circle me-2"></i>Add Channel</h5>
                    </div>
                    <div class="card-body">
                        <% if (!servers || servers.length===0) { %>
                            <div class="alert alert-warning border-0 bg-warning bg-opacity-10 text-warning">
                                <i class="bi bi-exclamation-triangle me-2"></i>Connect a server first.
                                <a href="/servers" class="btn btn-sm btn-warning w-100 mt-2">Go to Servers</a>
                            </div>
                            <% } else { %>
                                <form action="/channels" method="POST">
                                    <div class="mb-3">
                                        <label for="server_id"
                                            class="form-label text-muted small text-uppercase fw-bold">Server</label>
                                        <select class="form-select bg-dark text-light border-secondary" id="server_id"
                                            name="server_id" required>
                                            <option value="" selected disabled>Select a server</option>
                                            <% servers.forEach(server=> { %>
                                                <option value="<%= server.id %>">
                                                    <%= server.name %>
                                                </option>
                                                <% }); %>
                                        </select>
                                    </div>
                                    <div class="mb-3">
                                        <label for="channel_id"
                                            class="form-label text-muted small text-uppercase fw-bold">Channel
                                            ID</label>
                                        <input type="text" class="form-control" id="channel_id" name="channel_id"
                                            placeholder="123456789" required>
                                    </div>
                                    <div class="mb-3">
                                        <label for="webhook_url"
                                            class="form-label text-muted small text-uppercase fw-bold">Webhook
                                            URL</label>
                                        <input type="url" class="form-control" id="webhook_url" name="webhook_url"
                                            placeholder="https://discord.com/api/webhooks/..." required>
                                    </div>
                                    <button type="submit" class="btn btn-primary w-100">
                                        <i class="bi bi-plus-lg me-2"></i>Add Channel
                                    </button>
                                </form>
                                <% } %>
                    </div>
                </div>

                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0"><i class="bi bi-info-circle me-2"></i>Instructions</h5>
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <h6 class="text-light"><i class="bi bi-1-circle me-2 text-primary"></i>Get Channel ID</h6>
                            <p class="small text-muted mb-0">Enable Developer Mode > Right-click Channel > Copy ID</p>
                        </div>
                        <div class="mb-0">
                            <h6 class="text-light"><i class="bi bi-2-circle me-2 text-primary"></i>Get Webhook URL</h6>
                            <p class="small text-muted mb-0">Channel Settings > Integrations > Webhooks > Copy URL</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <footer class="mt-5 py-4 border-top border-secondary border-opacity-25">
        <div class="container text-center">
            <p class="mb-0 text-muted small">Discord Auto-Forwarder v0.1.0 &bull; Built with <i
                    class="bi bi-heart-fill text-danger mx-1"></i> by Benss</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
</body>

</html>